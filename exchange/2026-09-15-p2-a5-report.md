# A5 report: tool calling and structured output, the non-kernel half

Brief `briefs/2026-09-15-p2-a5-grammar-api.md`. Reports to `w82:p4`. Commit
`0950c6f` (`serve/src/main.rs`, `serve/src/text.rs`, `serve/Cargo.toml`,
`serve/Cargo.lock`).

## The split, applied

`docs/NEXT-PLAN.md` A5 has a device half (wire the mask into
`kernels/sample.mojo`, mask before Gumbel) and a host/API half. Kernel files
were never touched. Where the device boundary was actually hit
(`response_format` enforcement) is written up below and a `KERNEL:` message
was sent, not guessed at.

## Test fixture: a real Qwythos tokenizer + chat template

The q4 engine pack (`.work/engine-pack-q4`) ships no `tokenizer.json`, so
`baro-serve` ran with text endpoints disabled and nothing here was
testable end to end. Built one from the latest self-describing bake
(`~/Models/qwythos-9b-claude-mythos-5-1m-mtp-bf16/Qwythos-9B-Claude-Mythos-5-1M-MTP-BF16-BARO-8184f7d.gguf`,
pre-type `qwen35`, already supported by `tools/retired/gguf-tokenizer.py`,
no edits needed) and copied `tokenizer.json` / `tokenizer-meta.json` into
`.work/engine-pack-q4/` (additive, gitignored, does not touch `pack.bin`).
Its `tokenizer.chat_template` (7950 chars, extracted straight from the
gguf) carries `enable_thinking` and a full `tools`/`tool_call` block, so it
covers both `chat_template_kwargs` and `tools` on the repo's own model --
Granite's `enable_thinking`/`low_effort` (the brief's worked example) was
not needed and not used.

## Item 1: `chat_template_kwargs`

`ChatReq` gains `chat_template_kwargs: Option<Value>`; `Text::render`
merges its keys into the Jinja context verbatim. Qwythos's template:

```jinja
{%- if enable_thinking is defined and enable_thinking is false %}
    {{- '<think>\n\n</think>\n\n' }}
{%- else %}
    {{- '<think>\n' }}
{%- endif %}
```

Check: a debug `eprintln!` of the rendered prompt (no endpoint returns it
directly; the brief's own suggested check), two live requests through a
running `baro-serve` (`.work/a5-engine`, q4 pack), same message, with and
without `chat_template_kwargs`:

```
curl -s -X POST $URL/v1/chat/completions -H 'Content-Type: application/json' \
  -d '{"messages":[{"role":"user","content":"Name one color."}],"max_tokens":1}'
```
Rendered (195 chars):
```
<|im_start|>system
You are Qwythos, a model created by Empero AI. Only bring up your identity if the user asks.<|im_end|>
<|im_start|>user
Name one color.<|im_end|>
<|im_start|>assistant
<think>
```
Response: `{"content":"1", ...}` (continuing a reasoning chain).

```
curl -s -X POST $URL/v1/chat/completions -H 'Content-Type: application/json' \
  -d '{"messages":[{"role":"user","content":"Name one color."}],"max_tokens":1,"chat_template_kwargs":{"enable_thinking":false}}'
```
Rendered (206 chars):
```
<|im_start|>system
You are Qwythos, a model created by Empero AI. Only bring up your identity if the user asks.<|im_end|>
<|im_start|>user
Name one color.<|im_end|>
<|im_start|>assistant
<think>

</think>

```
Response: `{"content":"Blue", ...}` (answers directly, no open reasoning
block).

The two rendered prompts differ exactly at the `<think>` tail, and the
completions differ accordingly (a reasoning-chain token vs a direct
answer) -- the field reaches the render and changes what gets generated.
Full request/response bodies: `.work/a5-server-test/case1a.json`,
`case1b.json`; rendered prompts in `.work/a5-server-test/server.stderr`.

Unit tests (`serve/src/text.rs`, `cargo test`): `chat_template_kwargs_reach_the_render_context`.

## Item 2: `tools`

`ChatReq` gains `tools: Option<Value>`, handed to the template verbatim
(`{% if tools %}` renders a `<tools>...</tools>` system block, each entry
via `|tojson`). `ChatMsgIn` gains `tool_calls: Option<Vec<ToolCallIn>>`
(OpenAI shape, `arguments` a JSON string) and `tool_call_id` (accepted,
unused -- Qwythos's template keys a tool result off message order and
`role == "tool"`, not this id, checked directly in the template text).

Qwythos's own tool-call wire format, read from its `chat_template`, is
**not** OpenAI's `<tool_call>{"name":...}</tool_call>` JSON convention; it
is a Hermes-style XML format:
```
<tool_call>
<function=NAME>
<parameter=P1>
V1
</parameter>
</function>
</tool_call>
```
`parse_tool_calls`/`parse_one_tool_call` in `main.rs` parse this (one or
more, back to back) into `choices[0].message.tool_calls` in the OpenAI
response shape (`arguments` re-encoded as a JSON string). A parameter value
that parses as JSON keeps that type (number/bool/object/array); anything
else stays a string. `tool_calls_for_template` does the reverse conversion
for an incoming assistant message's `tool_calls` (arguments string ->
object), since the template reads `tool_call.arguments|items`.

**Real round trip**, `baro-serve` on the q4 pack, `.work/a5-server-test-turn2.sh`:

Turn 1:
```
curl -s -X POST $URL/v1/chat/completions -H 'Content-Type: application/json' -d '{
  "messages":[{"role":"user","content":"What is the weather in Paris? Use the get_weather tool."}],
  "max_tokens":128,"temperature":0,"chat_template_kwargs":{"enable_thinking":false},
  "tools":[{"type":"function","function":{"name":"get_weather",
    "description":"Get the current weather for a city",
    "parameters":{"type":"object","properties":{"location":{"type":"string"},
      "unit":{"type":"string","enum":["celsius","fahrenheit"]}},"required":["location"]}}}]}'
```
Response (trimmed):
```json
{"choices":[{"finish_reason":"stop","index":0,
  "message":{"content":"","role":"assistant",
    "tool_calls":[{"function":{"arguments":"{\"location\":\"Paris\"}","name":"get_weather"},
                   "id":"call_0","type":"function"}]}}],
 "id":"chatcmpl-1", ...}
```

Turn 2, the tool result appended as `role: "tool"`, plus the assistant's
own prior `tool_calls` echoed back (a real client's shape):
```
curl -s -X POST $URL/v1/chat/completions -H 'Content-Type: application/json' -d '{
  "messages":[
    {"role":"user","content":"What is the weather in Paris? Use the get_weather tool."},
    {"role":"assistant","content":"","tool_calls":[{"id":"call_0","type":"function",
      "function":{"name":"get_weather","arguments":"{\"location\":\"Paris\"}"}}]},
    {"role":"tool","tool_call_id":"call_0","content":"{\"temperature_c\": 15, \"condition\": \"sunny\"}"}],
  "max_tokens":64,"temperature":0,"chat_template_kwargs":{"enable_thinking":false},
  "tools":[{"type":"function","function":{"name":"get_weather", ... }}]}'
```
Response:
```json
{"choices":[{"finish_reason":"stop","index":0,
  "message":{"content":"The weather in Paris is sunny with a temperature of 15°C.",
             "role":"assistant"}}], "id":"chatcmpl-2", ...}
```

The model called `get_weather({"location":"Paris"})`, the parsed call was
sent back as `choices[0].message.tool_calls`, and after the tool result was
appended the model produced a correct final answer synthesizing it. Full
bodies: `.work/a5-server-test/case2-turn1.json`, `case2-turn2.json`, server
log `.work/a5-server-test/server-t2.stderr`.

**Out of scope, named rather than half-built:** streaming tool-call deltas.
OpenAI's real streaming protocol sends incremental per-argument-byte
deltas; this lane's `tools`/`response_format` work only touches the
non-streaming path. The SSE path renders `tools` into the prompt exactly
the same (same `apply_chat_template` call) but does not parse tool calls
out of the stream.

Unit tests (`serve/src/main.rs`, `cargo test`):
`parses_one_tool_call_with_typed_and_string_parameters`,
`parses_multiple_back_to_back_tool_calls`, `plain_text_has_no_tool_calls`,
`tool_calls_response_json_encodes_arguments_as_a_string`,
`tool_calls_for_template_parses_the_openai_string_back_to_an_object`;
`serve/src/text.rs`: `tools_array_reaches_the_template`.

## Item 3: `response_format`

`ChatReq` gains `response_format: Option<Value>`. `check_response_format`
validates the shape (`{"type":"json_schema","json_schema":{"schema":...}}`)
and always 400s, distinctly worded for a malformed shape vs a well-formed
one -- never silently ignored (`bench/PROTOCOL-RULES.md` P1).

**Why enforcement is not wired, the evidence:** `serve/engine.mojo`'s
decode loop (read directly, not assumed) gives the host per-step
visibility into the *already-chosen* token id only -- the same
synchronize-and-readback the M2 stop-sequence check already pays for
(`serve/engine.mojo:607-626`). There is no point where the host sees a
step's logits, and no mechanism to make the device choose a *different*
token once it has committed to one (`force_tok_h`/`BARO_FORCE` writes a
token into the KV/token buffer directly, but only for a pre-known,
fully-determined sequence -- teacher forcing for an identity gate, not a
reactive per-step decision; repurposing it into a rejection-sampling loop
would itself be a decode-loop architecture change). True enforcement needs
either the device mask (`kernels/sample.mojo`, mask before Gumbel -- the
device half of A5) or that host round trip, and both are out of an
API-wiring lane's scope. `KERNEL:` message sent to `w82:p4` with the exact
interface, below.

**"compile the schema through grammar/json_schema.mojo" is proven
separately from the live 400,** since there is no Rust-to-Mojo bridge for
`grammar/` today (nothing in either `serve/engine.mojo` or `serve/src/*.rs`
calls it; it is a tested-in-isolation library, per the brief's own
framing). Ran the existing suite instead of writing a redundant new one:

```
./.venv/bin/mojo build grammar/test_corpus.mojo -I . -o .work/test_grammar_test_corpus
./.work/test_grammar_test_corpus
```
```
schemas: 32 samples: 3200 failures: 0
PASS
```
Two of those 32 schemas are exactly `response_format`/tool-argument shaped:
`grammar/corpus/21_weather_tool_call.json`
(`{"type":"object","properties":{"location":{"type":"string"},"unit":{"type":"string","enum":["celsius","fahrenheit"]}},"required":["location"]}`,
the same shape as the live `get_weather` tool above) and
`grammar/corpus/32_tool_call_full.json` (a `{name, arguments}` envelope).
Each compiled through `compile_root_schema` and passed 100/100 sampled,
mask-filled, externally-validated draws. Also ran `test_reasoning_boundary`
and `test_rollback`: both `PASS`, unchanged by this lane, no compile
regression from anything here (none of this lane's code is on that call
path, run as a baseline sanity check).

Live 400, `response_format` shaped like the same weather schema:
```
curl -s -o /dev/null -w '%{http_code}' -X POST $URL/v1/chat/completions -H 'Content-Type: application/json' -d '{
  "messages":[{"role":"user","content":"give me json"}],"max_tokens":8,
  "response_format":{"type":"json_schema","json_schema":{"name":"weather",
    "schema":{"type":"object","properties":{"location":{"type":"string"},
      "unit":{"type":"string","enum":["celsius","fahrenheit"]}},"required":["location"]}}}}'
```
`400`, body:
```json
{"error":{"code":400,"message":"response_format is not enforced yet: no host-side point exists to apply a token mask without either kernels/sample.mojo (mask before Gumbel, the device half of A5) or a per-token host round trip that is itself a decode-loop architecture change; both are out of this lane's scope (briefs/2026-09-15-p2-a5-grammar-api.md). grammar/json_schema.mojo compiling this shape of schema is proven offline, see exchange/2026-09-15-p2-a5-report.md","type":"invalid_request_error"}}
```
Full body: `.work/a5-server-test/case3.json`.

Unit tests: `response_format_rejects_unsupported_type`,
`response_format_400s_a_well_formed_json_schema_shape_as_not_enforced`.

## Gates

- **T=0 identity unchanged, no grammar/tools/template kwargs**:
  `tools/test_server.sh` (chosen over `bench/force-ab.sh` -- it is the
  standing gate for the exact surface this lane touches,
  `serve/src/main.rs`/`text.rs`, and already runs the completion and chat
  cases at default sampling). `ALL PASS`, 25/25 cargo tests, clippy clean;
  `completion` and `chat` both 64/64 tokens matching the pack's own
  `ref-tokens-64.txt` (`.work/server-test/SUMMARY.txt`).
- **Schema-valid JSON on enforced prompts**: **UNVERIFIED, enforcement is
  not wired** (see item 3). What is missing: the `KERNEL:` interface below,
  or a host round-trip architecture change, either of which is out of this
  lane's scope.
- **Tool round trip, bodies pasted**: done, item 2 above, real two-turn
  round trip through a running `baro-serve`.
- **`./run-tests.sh` and `tools/ci-checks.sh` green at every commit**: both
  green at `0950c6f` (`run-tests.sh` 19.1 s including
  `kernels/test_latent.mojo`'s round trip; `ci-checks.sh` all non-GPU
  checks passed, 26 bench sources build, vendored-package drift checks
  clean).

## GPU minutes

- `.work/a5-server-test.sh` (case 1/2/3, hit the `tojson` bug on tools): 33.0 s
- rebuild with `minijinja` `json` feature, rerun: 32.4 s
- `.work/a5-server-test-turn2.sh` (full tool round trip): 33.0 s
- `./run-tests.sh`: 19.1 s
- `tools/test_server.sh`: 56.7 s

Total 174.2 s (2.9 GPU minutes) across 5 launches, all under the brief's
10-minute-per-gate ceiling; `gpu-wait list` checked clean before each
launch, one run queued briefly behind a peer job (the opus lane's PCIe
probe) and started once it cleared, as expected.

## Follow-up not done here (named, not silent)

- Wiring `grammar/` tests into `run-tests.sh`/`ci-checks.sh` as a standing
  gate: not asked for by this brief, not done; they currently run only when
  invoked by hand, same as before this lane.
- Streaming tool-call deltas (item 2, above).
- `response_format` enforcement (item 3): blocked on the `KERNEL:` message
  or a host round-trip design decision, neither this lane's call.
