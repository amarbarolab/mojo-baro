# Pre-tokenizer regexes that differ from llama.cpp's on purpose. name: reason. tools/pretok-check.py
# reports these as KNOWN-DIFF instead of failing; delete a line to make the check enforce parity again.
gpt-2: ours keeps the original GPT-2 trailing `|\s+` alternative that llama.cpp dropped; unreachable after `\s+(?!\S)` except on whitespace runs before end of text, where both produce the same pieces (61/61 tokenizer gate)
granite-docling: same regex as gpt-2 above
default: llama.cpp's switch fallback is a 4-regex sequence (punctuation group, gpt2 core, digit runs, 3-digit groups) for files with no tokenizer.ggml.pre; ours maps default to the gpt-2 regex, and no served model has an unset pre
